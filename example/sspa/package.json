{
  "scripts": {
    "build": "npm run clean && node bin/render.js",
    "clean": "rm -rf dist/*"
  },
  "dependencies": {
    "fs-walk": "0.0.1",
    "glob": "^5.0.13",
    "hyperstream": "^1.2.2",
    "writestreamp": "^0.1.0"
  }
}
